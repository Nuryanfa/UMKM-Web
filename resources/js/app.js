// resources/js/app.js

// Import Bootstrap JS dengan semua fungsionalitasnya
// Pastikan Bootstrap sudah terinstal via npm (npm install bootstrap)
import * as bootstrap from "bootstrap";

// Anda bisa menambahkan kode JavaScript kustom Anda di sini.
// Contoh sederhana:
document.addEventListener("DOMContentLoaded", function () {
    console.log("Aplikasi JavaScript dimulai!");

    // Contoh: Tangani klik pada elemen dengan ID 'myButton'
    // const myButton = document.getElementById('myButton');
    // if (myButton) {
    //     myButton.addEventListener('click', function() {
    //         alert('Tombol diklik!');
    //     });
    // }
});

// Jika Anda memiliki JS lain yang perlu di-global-kan, Anda bisa menambahkannya di window
// window.myGlobalFunction = function() {
//     console.log('Fungsi global dipanggil!');
// };
